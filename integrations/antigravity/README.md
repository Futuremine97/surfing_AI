# Surfing AI for Antigravity

Install the plugin from this checkout:

```bash
agy plugin install integrations/antigravity
```

Then launch Antigravity in the target project and invoke `/surfing-team`.
The parent agent will define and invoke parallel explorer, builder, and
verifier subagents under Antigravity's inherited permission policy.
